// TODO = change to your chain and contract settings
const chainID = 4369;
const rpcUrl = "http://localhost:9933/";
const abi = [
  // Some details about the token
  "function increment()",
  "function get() public view returns (uint)",
];

const contractAddress = "paste your contract address here";
